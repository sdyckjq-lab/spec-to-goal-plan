interface:
  display_name: Spec To Goal Plan
  short_description: Turn a finished spec into a /goal-ready execution plan.
  default_prompt: "Use $spec-to-goal-plan to review my finished spec, size the task, and produce the execution plan, progress ledger, and /goal starter prompt."
